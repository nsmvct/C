#include <stdio.h>

int main() {
    // Print a simple message to the screen
    printf("Hello, World!\n");
    
    // Print the text and then show how many characters it has
    printf(" -> Total characters: %d\n", printf("hello world\n"));
    
    return 0;
}
